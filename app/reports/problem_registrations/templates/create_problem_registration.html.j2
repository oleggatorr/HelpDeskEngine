{% extends "base.html" %}
{% set title = "Создать регистрацию проблемы — Help Desk Engine" %}

{% block content %}
{% include 'partials/left_side_menu.html' %}

<div class="container mt-4">
    <h1 class="mb-4">Создать регистрацию проблемы</h1>

    {% if error %}
    <div class="alert alert-danger">{{ error }}</div>
    {% endif %}

    <form method="post" action="/reports/problem-registrations/create" enctype="multipart/form-data">
        {# Поля ProblemRegistration #}
        <div class="card mb-3">
            <div class="card-header"><h6 class="mb-0">Регистрация проблемы</h6></div>
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">Тема</label>
                        <input type="text" name="subject" class="form-control"
                               value="{{ form_data.subject if form_data else '' }}" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Дата обнаружения</label>
                        <input type="datetime-local" name="detected_at" class="form-control"
                               value="{{ form_data.detected_at if form_data else now().strftime('%Y-%m-%dT%H:%M') }}">
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Локация</label>
                        <select name="location_id" class="form-select">
                            <option value="">— Не выбрано —</option>
                            {% for loc in locations %}
                            <option value="{{ loc.id }}" {% if form_data and form_data.location_id == loc.id %}selected{% endif %}>{{ loc.name }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Номенклатура</label>
                        <input type="text" name="nomenclature" class="form-control"
                               value="{{ form_data.nomenclature if form_data else '' }}">
                    </div>
                    <div class="col-12">
                        <label class="form-label">Описание</label>
                        <textarea name="description" class="form-control" rows="4">{{ form_data.description if form_data else '' }}</textarea>
                    </div>
                </div>
            </div>
        </div>

        {# Вложения #}
        <div class="card mb-3">
            <div class="card-header"><h6 class="mb-0">Вложения</h6></div>
            <div class="card-body">
                <div class="mb-2">
                    <input type="file" name="files" class="form-control" multiple accept="*/*">
                    <div class="form-text">Можно выбрать несколько файлов</div>
                </div>
            </div>
        </div>

        {# Поля Document #}
        <div class="card mb-3">
            <div class="card-header"><h6 class="mb-0">Параметры документа</h6></div>
            <div class="card-body">
                <div class="row g-3">
                    <input type="hidden" name="doc_status" value="open">
                    <div class="col-md-6">
                        <label class="form-label">Язык</label>
                        <select name="doc_language" class="form-select">
                            {% for lang in languages %}
                            <option value="{{ lang }}" {% if form_data and form_data.doc_language == lang %}selected{% elif not form_data and lang == 'ru' %}selected{% endif %}>{{ lang }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Приоритет</label>
                        <select name="doc_priority" class="form-select">
                            {% for p in priorities %}
                            <option value="{{ p }}" {% if form_data and form_data.doc_priority == p %}selected{% elif not form_data and p == 'medium' %}selected{% endif %}>{{ p }}</option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
            </div>
        </div>

        <div class="d-flex gap-2">
            <button type="submit" class="btn btn-primary">Создать</button>
            <a href="/reports/problem-registrations" class="btn btn-secondary">Отмена</a>
        </div>
    </form>
</div>
{% endblock %}
